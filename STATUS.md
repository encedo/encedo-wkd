# encedo-wkd — Deployment Status

## Status: PRODUCTION ✅

Last updated: 2026-04-08

---

## Server

- **Host:** mailserver.encedo.com (`65.21.170.222`)
- **Service:** `/etc/systemd/system/encedo-wkd.service` — running, enabled
- **Python files:** `/opt/encedo-wkd/`
- **Key cache:** `/var/encedo-wkd/cache/`
- **Config:** `/opt/encedo-wkd/config.json`
- **Port:** `127.0.0.1:8089`
- **Auth mode:** Carbonio (`carbonio_url = http://127.0.0.1:8080`), with Cookie fallback

## WKD Domains

| Domain | DNS CNAME | Cert | Test |
|--------|-----------|------|------|
| openpgpkey.encedo.com | ✅ | ✅ Let's Encrypt | ✅ 200 OK |
| openpgpkey.kress-net.com | ✅ | ✅ Let's Encrypt | ✅ 200 OK |
| openpgpkey.eight-lex.pl | ✅ | ✅ Let's Encrypt | ✅ 200 OK |

## Tests

- `curl https://openpgpkey.encedo.com/.well-known/openpgpkey/policy` → `200 OK` ✅
- `gpg --auto-key-locate clear,wkd --locate-key krzysztof@encedo.com` → key retrieved ✅

---

## nginx Architecture

Carbonio nginx (`/opt/zextras/common/sbin/nginx`):

```
nginx.conf.web.https           ← our file (generated by inject.sh)
  server { listen 65.21.170.222:443 ssl; server_name openpgpkey.*; }
  → proxy_pass http://127.0.0.1:8089

nginx.conf.web.https.default   ← Carbonio default
  server { listen 65.21.170.222:443 default_server; server_name mailserver.*; }
  → /wkd/ location → upstream wkd_server → 127.0.0.1:8089
```

**Key point:** `listen 65.21.170.222:443` (not `*:443`) — must use the same socket
as Carbonio's `default_server`, otherwise SNI routing does not work.

nginx worker tmp dir ownership note: running `nginx -t` as root causes workers to create
`/opt/zextras/data/tmp/nginx/client/` as `nobody`, breaking proxy buffering → HTTP 500.
Fix: after any nginx reload run `chown -R zextras:zextras /opt/zextras/data/tmp/nginx/`
(this is done automatically by `encedo-wkd-nginx-inject.sh`).

## Operational Files

| File | Description |
|------|-------------|
| `install.sh` | Full installation on a new server |
| `encedo-wkd-nginx-inject.sh` | nginx config + TLS certs (run after install.sh or domain changes) |
| `encedo-wkd.service` | systemd unit file |
| `nginx/upstream-wkd.conf` | Installed to `/opt/zextras/conf/nginx/extensions/` |
| `nginx/backend-wkd.conf` | Installed to `/opt/zextras/conf/nginx/extensions/` |

## Re-deployment (after Carbonio upgrade or adding a new domain)

```bash
sudo ./encedo-wkd-nginx-inject.sh
```

The script automatically:
1. Fetches domain list from Carbonio (`zmprov gad`)
2. Checks DNS CNAME for each `openpgpkey.*` subdomain
3. Requests Let's Encrypt certificates for new domains (if missing)
4. Detects server IP from `nginx.conf.web.https.default`
5. Generates nginx config and reloads nginx
6. Generates `config.json`
7. Fixes nginx tmp dir ownership (`chown -R zextras:zextras /opt/zextras/data/tmp/nginx/`)
